function ( err ) {
                                    if( err ) return callback( err );
                                    symlink( keyValues.version, path.join( prefix, UVx ), callback );
                                }